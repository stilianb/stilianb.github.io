import Head from "next/head";
import Namecard from "~/components/Namecard";
import Projects from "~/components/Projects";
import socials from "../api/socials.json";
import Socials from "~/components/Socials";
import Link from "next/link";

export default function Home() {
  return (
    <>
      <Head>
        <title>Stilian Balasopoulov</title>
        <meta name="my portfolio" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex flex-grow flex-col items-center justify-center gap-2 md:flex-row ">
        <Namecard />
        <div className="flex h-full w-full flex-col justify-center gap-4 rounded-md md:max-w-96 ">
          <div className="flex gap-2 rounded-md bg-mantle p-2">
            {socials.map((i) => (
              <Socials name={i.name} link={i.link} icon={i.icon} key={i.key} />
            ))}
          </div>
          <Projects />
        </div>
      </main>
    </>
  );
}
